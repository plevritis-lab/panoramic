# Retired helper placeholder.
# `panoramic_interpret_meta_mv()` was removed from the public API during
# workflow simplification because it was unused and overlapped with explicit
# table extractors.
