% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/panoramic-workflow.R
\name{panoramic_analyze}
\alias{panoramic_analyze}
\title{Run PANORAMIC end-to-end, including pooling/meta-analysis}
\usage{
panoramic_analyze(
  spe_list,
  design,
  cell_type = "cell_type",
  pairs = "auto",
  radii_um,
  stat = "local_comp_enrichment",
  nsim = 100,
  correction = NULL,
  min_cells = 5L,
  concavity = 50,
  window = c("concave", "convex", "rect"),
  group_col = "group",
  group_tau2 = c("none", "separate"),
  patient_col = NULL,
  sample_col = NULL,
  tau_structure = c("patient", "patient_sample"),
  method_mv = "REML",
  vi_floor = "group_median",
  seed = 123,
  boot = c("approx", "block"),
  tile_size = NULL,
  nx = NULL,
  ny = NULL,
  BPPARAM = BiocParallel::SerialParam(),
  verbose = FALSE
)
}
\arguments{
\item{spe_list}{Named list of SpatialExperiment objects (one per sample).}

\item{design}{data.frame with at least columns \code{sample} and \code{group}.}

\item{cell_type}{Character; colData column containing cell-type labels.}

\item{pairs}{Either \code{"auto"} or a data.frame with columns \code{ct1}, \code{ct2}.}

\item{radii_um}{Numeric vector of radii in microns.}

\item{stat}{Character spatial statistic passed to \code{panoramic_spatialstats()}.}

\item{nsim}{Integer bootstrap replicates.}

\item{correction}{Optional edge-correction method for spatstat-based
statistics. Ignored when \code{stat = "local_comp_enrichment"}.}

\item{min_cells}{Minimum cells per type per sample.}

\item{concavity}{Concavity for concave hull windows.}

\item{window}{One of \code{"concave"}, \code{"convex"}, or \code{"rect"}.}

\item{group_col}{Character group column for meta-analysis (defaults to \code{"group"}).}

\item{group_tau2}{Controls whether PANORAMIC additionally computes
group-specific heterogeneity (\code{tau2}) by fitting per-group multilevel
models. Use \code{"none"} for faster fitting or \code{"separate"} for more
detailed heterogeneity summaries.}

\item{patient_col}{Optional patient-id column for multilevel pooling. If
\code{NULL}, PANORAMIC uses \code{"patient"} from \code{design} when
available, otherwise falls back to \code{sample_col}.}

\item{sample_col}{Optional sample-id column used in
\code{panoramic_meta_mv()}. If \code{NULL}, PANORAMIC uses
\code{"sample"} (aligned to \code{design$sample}).}

\item{tau_structure}{Random-effects structure passed to
\code{panoramic_meta_mv()}.}

\item{method_mv}{Method passed to \code{metafor::rma.mv()} in \code{panoramic_meta_mv()}.}

\item{vi_floor}{Variance-flooring mode in \code{panoramic_meta_mv()}.
Default is \code{"group_median"}. Use \code{"median"} to replace
non-positive \code{vi} with per-feature median positive \code{vi}, or
\code{"none"} to disable flooring.}

\item{seed}{Optional random seed.}

\item{boot}{Bootstrap mode \code{"approx"} or \code{"block"}.}

\item{tile_size}{Optional tile size for block bootstrap.}

\item{nx, ny}{Optional tile counts for block bootstrap.}

\item{BPPARAM}{BiocParallel backend.}

\item{verbose}{Logical verbosity passed to \code{panoramic_spatialstats()}.}
}
\value{
A list with:
\itemize{
\item \code{prep}: output of \code{panoramic_prepare()}
\item \code{stats}: output of \code{panoramic_spatialstats()}
\item \code{pooled}: output of \code{panoramic_meta_mv()}
\item \code{tables}: pre-flattened data.frames for convenient result extraction
}
}
\description{
\code{panoramic_analyze()} provides a streamlined API for the most common
workflow: prepare data, compute spatial statistics, and run
pooling/meta-analysis in one call.
}
\examples{
toy <- panoramic_simulate_dataset(seed = 1)
out <- panoramic_analyze(
  spe_list = toy$spe_list,
  design = toy$design,
  cell_type = "cell_type",
  radii_um = c(10, 20),
  stat = "local_comp_enrichment",
  nsim = 5,
  min_cells = 2,
  window = "rect",
  group_col = "group",
  BPPARAM = BiocParallel::SerialParam()
)
names(out)

}
